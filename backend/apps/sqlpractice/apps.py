from django.apps import AppConfig

class SqlpracticeConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.sqlpractice'
    verbose_name = 'SQL Sandbox & Practice Environment'
